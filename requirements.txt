torch>=2.0.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10
flask>=3.0
flask-cors>=4.0
jupyter>=1.0
tqdm>=4.65
joblib>=1.3

# ---- optional: real SPICE backend ----
# ngspice must be installed separately (https://ngspice.sourceforge.io/)
#   Windows: choco install ngspice   |   Linux: sudo apt install ngspice
# PySpice>=1.5    # optional Python interface (not required; we call ngspice directly)
